// Типы данных в TypeScript

// Число
let number: number = 4;

// Строка
let string: string = "Four";

// Буль
let boolean: boolean = false;

// Undefined
let u: undefined = undefined;

//Null
let n: null = null;

// Объект
let obj: object = { a: 1 };
